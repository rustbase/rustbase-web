# Installation

To install the Rustbase and the CLI, run the following command:

```bash
$ curl -L https://www.rustbase.app/install | bash
```

## Warnings

-   The Rustbase and the Rustbase CLI is currently in beta. It is not recommended to use it in production.
-   Currently Rustbase and the Rustbase CLI only supports x64 Linux.
